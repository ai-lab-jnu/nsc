# 건강보험 MySQL DDL


- 건강보험 활용 데이터의 MySQL 생성 스크립트
    - [건강보험 빅데이터 활용 교육 실습 자료  기반](https://nhiss.nhis.or.kr/bbs/boards/notice/963.do)


```
CREATE TABLE `nsc2_bnc_1000` (
  `RN_INDI` int(11) DEFAULT NULL COMMENT '개인고유번호(7자리), 연계변수 (주민등록번호의 대체 식별번호)',
  `STD_YYYY` int(11) DEFAULT NULL COMMENT '기준년도 (2002~2015년 자격 및 의료이용 해당년도)',
  `SEX` int(11) DEFAULT NULL COMMENT '성별 (1: 남자, 2: 여자)',
  `SGG` int(11) DEFAULT NULL COMMENT '시군구\n\n시도 2자리와 시군구 3자리로 구성\n해당년도의 유효 시군구 정보',
  `GAIBJA_TYPE` int(11) DEFAULT NULL COMMENT '가입자 구분\n\n1: 지역세대주\n2: 지역세대원\n5: 직장가입자\n6: 직장피부양자\n7: 의료급여세대주\n8: 의료급여세대원',
  `CTRB_Q10` int(11) DEFAULT NULL COMMENT '보험료분위 10분위 (직장 및 지역가입자 각 10분위, 의료급여수급권자 0분위)',
  `DSB_SVRT_CD` char(1) COLLATE utf8_unicode_ci DEFAULT NULL COMMENT '장애중증도 구분\n\n0: 해당없음\n1: 중증 (1~2등급)\n2: 경증 (3~6등급)',
  `DSB_TYPE_CD` char(1) COLLATE utf8_unicode_ci DEFAULT NULL COMMENT '장애유형 구분\n\n주요 장애 (지체, 뇌병변, 시각, 청각장애) 외의 장애는 기타장애로 그룹화',
  `G1E_OBJ_YN` char(1) COLLATE utf8_unicode_ci DEFAULT NULL COMMENT '건강검진 (일반 1차) 대상자 여부 (Y: 대상자)',
  `SMPL_TYPE_CD` int(11) DEFAULT NULL
) ENGINE=InnoDB DEFAULT CHARSET=utf8 COLLATE=utf8_unicode_ci COMMENT='자격 및 보험료 테이블\n\n\n?   자격자료(사회경제적 변수) : 각 해당년도 연초 자격 기준\n- 건강보험가입자 및 의료급여 수급권자로 구성 (외국인 제외)\n?   직장 및 지역가입자 각 10분위, 의료급여수급권자 0분위(null 값)\n\n※ 직장, 지역의 같은 분위가 같은 소득수준을 뜻하는 것은 아님\n※ 의료급여 대상자의 보험료는 결측\n\n\n\n시군구코드\n\n?   구성(다섯 자리): 시도(두 자리) + 시군구(세 자리)\n예) 시군구 코드 ‘11110’: ‘11’(서울시) + ‘110’(종로구)\n- 시군구 코드는 해당시점 유효한 행정안전부 주민등록주소코드로 변환\n※ 시군구 행정구역 개편에 의한 연도별 행정구역의 변경사항을 검토하여 활용\n- 결측이거나 시도 단위까지만 존재하는 등 유효하지 않은 경우 가장 근접한 유효 시군\n구 코드로 대체\n- 연평균 대상자수가 500인 미만인 4개 군은 인접 군과 코드 통합\n';

CREATE TABLE `nsc2_bnd_1000` (
  `RN_INDI` int(11) DEFAULT NULL COMMENT '개인고유번호(7자리), 연계코드',
  `BTH_YYYY` varchar(10) COLLATE utf8_unicode_ci DEFAULT NULL COMMENT '출생연도',
  `DTH_YYYYMM` varchar(10) COLLATE utf8_unicode_ci DEFAULT NULL COMMENT '사망자의 사망월 ... 통계청 사망원인 연계',
  `COD1` varchar(10) COLLATE utf8_unicode_ci DEFAULT NULL COMMENT '한국표준질병?사인분류(KCD) 코드 사용\n\n- 범위 : A00-T98\n-  일반적인 사망(소분류), 민감사인(중분류)\n※ 일반적인 사망은(A00 ~ R99) 사망원인1만 있음\n※ 사망원인',
  `COD2` varchar(10) COLLATE utf8_unicode_ci DEFAULT NULL
) ENGINE=InnoDB DEFAULT CHARSET=utf8 COLLATE=utf8_unicode_ci COMMENT='출생 및 사망 테이블 개요\n\n? 내용 : 표본의 출생연도, 사망연월 및 사망원인 정보\n\n? 연령 :\n\n-  대상자의 출생년도로 연령 산출 시 ‘기준년도’-‘출생년도’계산 필요\n\n-  2006년 기준 85세 해당(1921년 및 그 이전 출생자)자 출생연도 ‘1921LE’\n';

CREATE TABLE `nsc2_d20_1000` (
  `RN_INDI` int(11) DEFAULT NULL COMMENT '개인고유번호(7자리), 연계코드',
  `RN_KEY` bigint(20) DEFAULT NULL COMMENT '청구고유번호 (14자리), 연계코드',
  `RN_INST` int(11) DEFAULT NULL COMMENT '요양기관고유번호(6자리), 연계코드',
  `MDCARE_STRT_DT` int(11) DEFAULT NULL COMMENT '요양 게시일자\n\n- 수진자가 진료를 받기 시작한 일자\n- 의과, 치과, 한방, 보건기관: 당월 요양 개시 일자 (월 통합 작성 명세서) 또는 내원 일자\n- 약국: 조제 투약 일자',
  `FORM_CD` varchar(10) COLLATE utf8_unicode_ci DEFAULT NULL COMMENT '서식코드\n\n요양급여비용심사(의료보호) 청수거 및 명세서의 진료 구분명세서 서식 구분',
  `MCARE_SUBJ_CD` int(11) DEFAULT NULL COMMENT '진료과목코드\n\n- (병원급 이상) 실제 진료를 받은 진료과목\n- (의원) 상병명에 해당하는 진료과목\n\n| 코드 | 진료과 |\n| --- | --- |\n| 00 | 일반의 |\n| 01 | 내과 |\n| 02 | 신경과 |\n| 03 | 정신과 | \n| 04 | 외과 | \n| 05 | 정형외과 | \n| 06 | 신경외과 | \n| 07 | 흉부외과 | \n| 08 | 성형외과 |\n| 09 | 마취통증의학과 | \n| 10 | 산부인과 |\n| 11 | 소아청소년과 |\n| 12 | 안과 | \n| 13 | 이비인후과 |\n| 14 | 피부과 |\n| 15 | 비뇨기과 |\n| 16 | 영상의학과 |\n| 17 | 방사선 종양학과 | \n| 18 | 병리과 |\n| 19 | 진단검사의학과 | \n| 20 | 결핵과 | \n| 21 | 재활의학과 | \n| 22 | 핵의학과 | \n| 23 | 가정의학과 | \n\n',
  `SICK_SYM1` varchar(10) COLLATE utf8_unicode_ci DEFAULT NULL COMMENT '주상병\n\n진료기간 중 치료나 검사 등에 대한 환자의 요구가 가장 컸던 상병',
  `SICK_SYM2` varchar(10) COLLATE utf8_unicode_ci DEFAULT NULL COMMENT '부상병\n\n진료기간 중 주상병과 함께 있었거나 발생된 상병으로 환자 진료에 영향을 주었던 상병',
  `FST_HSPTZ_DT` varchar(10) COLLATE utf8_unicode_ci DEFAULT NULL COMMENT '최초 입원일자\n\n입원 요양급여비용 분리청구의 경우 입원의 최초 입원개시일 기재',
  `HSPTZ_PATH_TYPE` varchar(10) COLLATE utf8_unicode_ci DEFAULT NULL COMMENT '입원 경로구분\n\n· 병원급 이상 입원 환자의 경우 기재\n· 1번째 자리(도착경로) 1: 타요양기관경유, 2: 응급구조대후송, 3: 기타\n· 2번째 자리(입원경로) 1 응급실, 2 외래\n',
  `OFIJ_TYPE` varchar(10) COLLATE utf8_unicode_ci DEFAULT NULL COMMENT '공상 등 구분\n\n· 공상(공무상 상해) 및 보훈, 군인, 차상위 희귀질환자 등에 해당되는\n적용대상자 구분 코드\n※ 각 제도별 대상자 선정기준 및 적용 내용은 관련 법령 및 고시 참고',
  `OPRTN_YN` int(11) DEFAULT NULL COMMENT '수술여부 (0: 미수술, 1: 수술)',
  `MDCARE_DD_CNT` int(11) DEFAULT NULL,
  `VSHSP_DD_CNT` int(11) DEFAULT NULL,
  `TOT_PRSC_DD_CNT` int(11) DEFAULT NULL COMMENT '총 처방일수\n\n· 처방전에 기재된 처방약품별 처방일수 중 가장 긴 처방일수\n· 처방일자별로 처방내역이 다른 경우에는 처방일수를 합산하여 기재\n',
  `MCARE_RSLT_TYPE` int(11) DEFAULT NULL COMMENT '진료결과구분\n\n· 최종진료일의 환자상태 구분\n· 1: 계속, 2: 이송, 3: 회송, 4 :사망, 9: 퇴원 또는 외래치료 종결\n',
  `EDC_ADD_RT` double DEFAULT NULL COMMENT '심결 가산율\n\n· 요양기관의 종별 규모에 따라 시설, 인력, 장비 등의 투자비용\n등을 고려하여 가산 적용되는 비율(%)\n※ 요양개시일자 기준의 가산율, 공상가산율, 의료급여가산율 적용\n',
  `ED_RC_TOT_AMT` int(11) DEFAULT NULL COMMENT '심결요양 급여 비용 총액\n\n· 심사결정 후 건강보험 총 요양 급여 비용',
  `EDC_SBA` int(11) DEFAULT NULL,
  `EDC_INSUR_BRDN_AMT` int(11) DEFAULT NULL COMMENT '심결 본인 부담금\n\n· 심사 결정 후 수진자가 부담해야 할 금액',
  `SPCF_SYM_TYPE` varchar(10) COLLATE utf8_unicode_ci DEFAULT NULL COMMENT '특정기호구분\n\n「본인일부부담금 산정특례에 관한 기준」 관련 특정기호 코드',
  `STD_YYYY` int(11) DEFAULT NULL
) ENGINE=InnoDB DEFAULT CHARSET=utf8 COLLATE=utf8_unicode_ci COMMENT='치과 - 진료 명세서\n\n';

CREATE TABLE `nsc2_d30_1000` (
  `RN_INDI` int(11) DEFAULT NULL COMMENT '개인고유번호(7자리), 연계코드',
  `RN_KEY` bigint(20) DEFAULT NULL COMMENT '청구고유번호 (14자리), 연계코드',
  `MDCARE_STRT_DT` int(11) DEFAULT NULL COMMENT '요양 게시일자\n\n- 수진자가 진료를 받기 시작한 일자\n- 의과, 치과, 한방, 보건기관: 당월 요양 개시 일자 (월 통합 작성 명세서) 또는 내원 일자\n- 약국: 조제 투약 일자',
  `FORM_CD` int(11) DEFAULT NULL COMMENT '서식코드\n\n요양급여비용심사(의료보호) 청수거 및 명세서의 진료 구분명세서 서식 구분',
  `MCARE_DESC_LN_NO` int(11) DEFAULT NULL COMMENT '줄번호\n\n처방전 발행건에 대한 세부내역 일련번호',
  `CLA_CD` int(11) DEFAULT NULL COMMENT '항코드 (요양 급여 비용 명세서 항(1~10) 또는 C,M,T 코드), 코드표 참고\n',
  `ITEM_CD` int(11) DEFAULT NULL COMMENT '목코드 (18개항의 소분류 단위로 부여된 번호),  코드표 참고',
  `CLSF_PTTN_CD` int(11) DEFAULT NULL COMMENT '분류유형코드\n\n명세서의 분류코드유형, 코드표 참고',
  `MCARE_DIV_CD` varchar(10) COLLATE utf8_unicode_ci DEFAULT NULL COMMENT '진료분류코드\n\n진료분류코드(MCARE_DIV_CD) : 요양급여비용 분류항목별 코드\n?   수가(행위)코드, 약품코드, 치료재료코드 등 진료내역 관련 고유코드\n\n§ 해당 분류코드 확인은 수가파일, 약가파일 등 해당 파일을 다운받아 사용\nㄴ 건강보험심사평가원 홈페이지(hira.or.kr)\n\n※  약제의 경우 처방전교부상세(60T)의 일반명 코드(GNL_NM_CD)와 동일한 값으로 구성\n-  수가(행위)코드, 의약품 제품코드, 재료대코드 등이 포함되는데, 이 중 의약품 제품코드는 제품마다의 고유번호이기 때문에, 특정 약품에 대한 시장조사 등 사익 목적으로 이용될 가능성이 있어 기존 진료분류코드를 일반명코드 값으로 대체하였음\n',
  `UPRC` int(11) DEFAULT NULL COMMENT '단가 상세설명참고',
  `DD1_MQTY_FREQ` int(11) DEFAULT NULL COMMENT '1일투여량또는실시횟수\n\n1일 투여량을 기재, 의약품 및 처방내역 의약품의 경우 1일 투약횟수 기재\n',
  `TOT_MCNT` int(11) DEFAULT NULL COMMENT '총투여일수 또는 실시횟수\n',
  `AMT` int(11) DEFAULT NULL COMMENT '금액\n\n단가 × (일일투여량 또는 실시횟수) × (총투여일수 또는 실시횟수)',
  `TIME1_MDCT_CPCT` int(11) DEFAULT NULL COMMENT '1회 투여 용량',
  `MDCN_UD` int(11) DEFAULT NULL COMMENT '약제상한차액\n\n약제의 상한가와 요양기관이 구입한 단가와의 차액 중\n100분의70에 해당하는 금액....※2010.11월 차수 추가\n',
  `MCEXP_TYPE_CD` int(11) DEFAULT NULL COMMENT '진료비 구분 코드\n\n1: 기본진료료\n2: 진료행위료\n3: 약품비\n4: 치료재료료\n5: 정액수가\n',
  `EFMDC_CLSF_NO` int(11) DEFAULT NULL COMMENT '약효 분류번호\n\n· (표본코호트 2.0 DB 추가) ‘주성분코드’ 활용 권장'
) ENGINE=InnoDB DEFAULT CHARSET=utf8 COLLATE=utf8_unicode_ci COMMENT='진료내역';

CREATE TABLE `nsc2_d40_1000` (
  `RN_INDI` int(11) DEFAULT NULL COMMENT '개인고유번호(7자리), 연계코드',
  `RN_KEY` bigint(20) DEFAULT NULL COMMENT '청구고유번호 (14자리), 연계코드',
  `MDCARE_STRT_DT` int(11) DEFAULT NULL COMMENT '요양 게시일자\n\n- 수진자가 진료를 받기 시작한 일자\n- 의과, 치과, 한방, 보건기관: 당월 요양 개시 일자 (월 통합 작성 명세서) 또는 내원 일자\n- 약국: 조제 투약 일자',
  `FORM_CD` varchar(10) COLLATE utf8_unicode_ci DEFAULT NULL COMMENT '서식코드\n\n요양급여비용심사(의료보호) 청수거 및 명세서의 진료 구분명세서 서식 구분',
  `MCEX_SICK_SYM` varchar(10) COLLATE utf8_unicode_ci DEFAULT NULL COMMENT '요양급여상병기호\n\n진료기간 중 환자가 진료 받은 해당상병',
  `DETAIL_TMSG_SUBJ_CD` varchar(10) COLLATE utf8_unicode_ci DEFAULT NULL COMMENT '세부전문과목코드\n',
  `SICK_CLSF_TYPE` varchar(10) COLLATE utf8_unicode_ci DEFAULT NULL COMMENT '상병분류구분코드\n\n· 각 상병기호별 주상병, 부상병, 배제된 상병을 구분하는 구분자\n1: 주상병\n2: 부상병\n3: 배제된 상병 (결측값도 존재)\n(조건 설정:  1, 2, 결측 값 모두 선정)\n',
  `STD_YYYY` int(11) DEFAULT NULL
) ENGINE=InnoDB DEFAULT CHARSET=utf8 COLLATE=utf8_unicode_ci COMMENT='상병내역';

CREATE TABLE `nsc2_d60_1000` (
  `RN_INDI` int(11) DEFAULT NULL COMMENT '개인고유번호(7자리), 연계코드',
  `RN_KEY` bigint(20) DEFAULT NULL COMMENT '청구고유번호 (14자리), 연계코드',
  `MDCARE_STRT_DT` int(11) DEFAULT NULL COMMENT '요양 게시일자\n\n- 수진자가 진료를 받기 시작한 일자\n- 의과, 치과, 한방, 보건기관: 당월 요양 개시 일자 (월 통합 작성 명세서) 또는 내원 일자\n- 약국: 조제 투약 일자',
  `FORM_CD` varchar(10) COLLATE utf8_unicode_ci DEFAULT NULL COMMENT '서식코드\n\n요양급여비용심사(의료보호) 청수거 및 명세서의 진료 구분명세서 서식 구분',
  `MCARE_DESC_LN_NO` int(11) DEFAULT NULL COMMENT '줄번호\n\n처방전발행건 세부내역 일련번호\n',
  `CLSF_PTTN_CD` int(11) DEFAULT NULL COMMENT '분류 유형코드\n\n명세서의 분류코드유형, 코드표 참고',
  `MPRSC_TIME1_TUYAK_CPCT` varchar(10) COLLATE utf8_unicode_ci DEFAULT NULL COMMENT '1회투여량횟수\n\n1회 투약량을 기재',
  `MPRSC_DD1_TUYAK_CPCT` int(11) DEFAULT NULL COMMENT '1일 투약량\n\n1회 투약량을 기재',
  `TOT_MCNT` int(11) DEFAULT NULL COMMENT '총 투여 일수',
  `UPRC` varchar(10) COLLATE utf8_unicode_ci DEFAULT NULL COMMENT '단가',
  `AMT` int(11) DEFAULT NULL COMMENT '금액 (1회 투여량 × 1일 투약량 × 총 투여일수 × 단가)',
  `GNL_NM_CD` varchar(10) COLLATE utf8_unicode_ci DEFAULT NULL,
  `EFMDC_CLSF_NO` int(11) DEFAULT NULL COMMENT '약효 분류번호 (진료내역(T30) 설명 참고)'
) ENGINE=InnoDB DEFAULT CHARSET=utf8 COLLATE=utf8_unicode_ci COMMENT='처방전 교부 상세';

CREATE TABLE `nsc2_g1e_0208_1000` (
  `RN_INDI` int(11) DEFAULT NULL COMMENT '개인고유번호(7자리), 연계코드',
  `EXMD_BZ_YYYY` int(11) DEFAULT NULL,
  `HME_YYYYMM` int(11) DEFAULT NULL,
  `Q_PHX1_DZ_V0208` int(11) DEFAULT NULL COMMENT '병력코드1 (1~9)',
  `Q_PHX1_YR` text COLLATE utf8_unicode_ci COMMENT '발생연도1',
  `Q_PHX1_CR` text COLLATE utf8_unicode_ci COMMENT '완치유무1 (1,2)',
  `Q_PHX2_DZ_V0208` int(11) DEFAULT NULL COMMENT '병력코드2 (1~9)',
  `Q_PHX2_YR` text COLLATE utf8_unicode_ci COMMENT '발생연도2',
  `Q_PHX2_CR` text COLLATE utf8_unicode_ci COMMENT '완치유무2 (1,2)',
  `Q_PHX3_DZ_V0208` int(11) DEFAULT NULL COMMENT '병력코드3 (1~9)',
  `Q_PHX3_YR` text COLLATE utf8_unicode_ci COMMENT '발생연도3',
  `Q_PHX3_CR` text COLLATE utf8_unicode_ci COMMENT '완치유무3 (1,2)',
  `Q_FHX_LVDZ` text COLLATE utf8_unicode_ci COMMENT '2002~2008년 문진<br/>(가족력) 간장질환유무 (1,2)',
  `Q_FHX_HTN` varchar(10) COLLATE utf8_unicode_ci DEFAULT NULL COMMENT '2002~2008년 문진<br/>(가족력) 심근경색유무 (1,2)',
  `Q_FHX_STK` text COLLATE utf8_unicode_ci COMMENT '2002~2008년 문진<br/>(가족력) 뇌졸증유무 (1,2)',
  `Q_FHX_HTDZ` text COLLATE utf8_unicode_ci COMMENT '2002~2008년 문진<br/>(가족력) 심장병유무 (1,2)',
  `Q_FHX_DM` char(1) COLLATE utf8_unicode_ci DEFAULT NULL COMMENT '2002~2008년 문진<br/>(가족력) 당뇨병유무 (1,2)',
  `Q_FHX_CC` text COLLATE utf8_unicode_ci COMMENT '2002~2008년 문진<br/>(가족력) 암유무 (1,2)',
  `Q_AWR_YN` char(1) COLLATE utf8_unicode_ci DEFAULT NULL COMMENT '(본인)과거 질환유무 (1,2)',
  `Q_AWR_DZ` text COLLATE utf8_unicode_ci COMMENT '(본인)과거 질환종류',
  `Q_NTR_PRF` int(11) DEFAULT NULL,
  `Q_DRK_FRQ_V0108` char(1) COLLATE utf8_unicode_ci DEFAULT NULL,
  `Q_DRK_AMT_V0108` char(1) COLLATE utf8_unicode_ci DEFAULT NULL,
  `Q_SMK_YN` char(1) COLLATE utf8_unicode_ci DEFAULT NULL COMMENT '2002~2008년 문진<br/>흡연상태<br/>1: 피우지 않는다<br/>2: 과거에 피웠으나 지금은 끊었다<br/>3: 현재도 피운다',
  `Q_SMK_NOW_AMT_V0108` char(1) COLLATE utf8_unicode_ci DEFAULT NULL,
  `Q_SMK_DRT` varchar(10) COLLATE utf8_unicode_ci DEFAULT NULL COMMENT '2002~2008년 문진<br/>(과거,현재)흡연기간<br/>1: 5년 미만<br/>2: 5~9년<br/>3: 10~19년<br/>4: 20~29년<br/>5: 30년 이상',
  `Q_SMK_STRT_YR` varchar(10) COLLATE utf8_unicode_ci DEFAULT NULL,
  `Q_SMK_STOP_YR` varchar(10) COLLATE utf8_unicode_ci DEFAULT NULL,
  `Q_PA_FRQ` char(1) COLLATE utf8_unicode_ci DEFAULT NULL COMMENT '2002~2008년 문진<br/>1주 운동횟수<br/>1: 안한다<br/>2: 1~2회<br/>3: 3~4회<br/>4: 5~6회<br/>5: 거의 매일',
  `G1E_HGHT` int(11) DEFAULT NULL COMMENT '신장',
  `G1E_WGHT` int(11) DEFAULT NULL COMMENT '체중',
  `G1E_BMI` double DEFAULT NULL,
  `G1E_WSTC` varchar(10) COLLATE utf8_unicode_ci DEFAULT NULL,
  `G1E_VA_LT` double DEFAULT NULL COMMENT '시력(좌)',
  `G1E_VA_RT` double DEFAULT NULL COMMENT '시력(우)',
  `G1E_HA_LT` char(1) COLLATE utf8_unicode_ci DEFAULT NULL,
  `G1E_HA_RT` char(1) COLLATE utf8_unicode_ci DEFAULT NULL,
  `G1E_BP_SYS` int(11) DEFAULT NULL,
  `G1E_BP_DIA` int(11) DEFAULT NULL,
  `G1E_URN_GLU` int(11) DEFAULT NULL,
  `G1E_URN_PROT` int(11) DEFAULT NULL,
  `G1E_URN_OCC_BLD` int(11) DEFAULT NULL,
  `G1E_URN_PH` int(11) DEFAULT NULL COMMENT '요PH<br/>pH<br/>* 0값 결측처리',
  `G1E_HGB` double DEFAULT NULL COMMENT '혈색소<br/>g/dL<br/>* 0값 결측처리',
  `G1E_FBS` int(11) DEFAULT NULL COMMENT '식전혈당(공복혈당)',
  `G1E_TOT_CHOL` int(11) DEFAULT NULL COMMENT '총콜레스테롤',
  `G1E_SGOT` int(11) DEFAULT NULL COMMENT '(혈청지오티)AST<br/>U/L<br/>*0 값 결측처리',
  `G1E_SGPT` int(11) DEFAULT NULL COMMENT '(혈청지피티)ALT<br/>U/L<br/>*0 값 결측처리',
  `G1E_GGT` int(11) DEFAULT NULL COMMENT '감마지티피<br/>U/L<br/>*0 값 결측처리',
  `G1E_CHST_XRAY_RST` int(11) DEFAULT NULL,
  `G1E_EKG_RST` char(1) COLLATE utf8_unicode_ci DEFAULT NULL
) ENGINE=InnoDB DEFAULT CHARSET=utf8 COLLATE=utf8_unicode_ci;

CREATE TABLE `nsc2_g1e_0208_2000` (
  `EXMD_BZ_YYYY` int(11) DEFAULT NULL,
  `RN_INDI` int(11) DEFAULT NULL,
  `HME_YYYYMM` int(11) DEFAULT NULL,
  `Q_PHX1_DZ_V0208` int(11) DEFAULT NULL,
  `Q_PHX1_YR` text COLLATE utf8_unicode_ci,
  `Q_PHX1_CR` text COLLATE utf8_unicode_ci,
  `Q_PHX2_DZ_V0208` int(11) DEFAULT NULL,
  `Q_PHX2_YR` text COLLATE utf8_unicode_ci,
  `Q_PHX2_CR` text COLLATE utf8_unicode_ci,
  `Q_PHX3_DZ_V0208` int(11) DEFAULT NULL,
  `Q_PHX3_YR` text COLLATE utf8_unicode_ci,
  `Q_PHX3_CR` text COLLATE utf8_unicode_ci,
  `Q_FHX_LVDZ` text COLLATE utf8_unicode_ci,
  `Q_FHX_HTN` varchar(10) COLLATE utf8_unicode_ci DEFAULT NULL,
  `Q_FHX_STK` text COLLATE utf8_unicode_ci,
  `Q_FHX_HTDZ` text COLLATE utf8_unicode_ci,
  `Q_FHX_DM` int(11) DEFAULT NULL,
  `Q_FHX_CC` text COLLATE utf8_unicode_ci,
  `Q_AWR_YN` int(11) DEFAULT NULL,
  `Q_AWR_DZ` text COLLATE utf8_unicode_ci,
  `Q_NTR_PRF` int(11) DEFAULT NULL,
  `Q_DRK_FRQ_V0108` int(11) DEFAULT NULL,
  `Q_DRK_AMT_V0108` text COLLATE utf8_unicode_ci,
  `Q_SMK_YN` int(11) DEFAULT NULL,
  `Q_SMK_NOW_AMT_V0108` text COLLATE utf8_unicode_ci,
  `Q_SMK_DRT` varchar(10) COLLATE utf8_unicode_ci DEFAULT NULL,
  `Q_SMK_STRT_YR` text COLLATE utf8_unicode_ci,
  `Q_SMK_STOP_YR` text COLLATE utf8_unicode_ci,
  `Q_PA_FRQ` int(11) DEFAULT NULL,
  `G1E_HGHT` int(11) DEFAULT NULL,
  `G1E_WGHT` int(11) DEFAULT NULL,
  `G1E_BMI` double DEFAULT NULL,
  `G1E_WSTC` text COLLATE utf8_unicode_ci,
  `G1E_VA_LT` double DEFAULT NULL,
  `G1E_VA_RT` double DEFAULT NULL,
  `G1E_HA_LT` int(11) DEFAULT NULL,
  `G1E_HA_RT` int(11) DEFAULT NULL,
  `G1E_BP_SYS` int(11) DEFAULT NULL,
  `G1E_BP_DIA` int(11) DEFAULT NULL,
  `G1E_URN_GLU` int(11) DEFAULT NULL,
  `G1E_URN_PROT` int(11) DEFAULT NULL,
  `G1E_URN_OCC_BLD` int(11) DEFAULT NULL,
  `G1E_URN_PH` int(11) DEFAULT NULL,
  `G1E_HGB` double DEFAULT NULL,
  `G1E_FBS` int(11) DEFAULT NULL,
  `G1E_TOT_CHOL` int(11) DEFAULT NULL,
  `G1E_SGOT` int(11) DEFAULT NULL,
  `G1E_SGPT` int(11) DEFAULT NULL,
  `G1E_GGT` int(11) DEFAULT NULL,
  `G1E_CHST_XRAY_RST` int(11) DEFAULT NULL,
  `G1E_EKG_RST` varchar(10) COLLATE utf8_unicode_ci DEFAULT NULL
) ENGINE=InnoDB DEFAULT CHARSET=utf8 COLLATE=utf8_unicode_ci;

CREATE TABLE `nsc2_g1e_0915_1000` (
  `RN_INDI` int(11) DEFAULT NULL COMMENT '개인고유번호(7자리), 연계코드',
  `EXMD_BZ_YYYY` int(11) DEFAULT NULL,
  `HME_YYYYMM` int(11) DEFAULT NULL,
  `Q_PHX_TX_DM` text COLLATE utf8_unicode_ci COMMENT '? 2009~2015년 문진<br/>(본인)당뇨병<br/>약물치료여부<br/>(0,1)',
  `Q_FHX_HTN` text COLLATE utf8_unicode_ci COMMENT '(가족력)심근경색유무 (1,2)\n',
  `Q_SMK_YN` int(11) DEFAULT NULL COMMENT '2002~2008년 문진<br/>흡연상태<br/>1: 피우지 않는다<br/>2: 과거에 피웠으나 지금은 끊었다<br/>3: 현재도 피운다',
  `Q_PHX_DX_HTN` text COLLATE utf8_unicode_ci COMMENT '? 2009~2015년 문진<br/>(본인)심근경색<br/>과거병력유무 (0,1)',
  `Q_PHX_TX_ETC` text COLLATE utf8_unicode_ci COMMENT '? 2009~2015년 문진<br/>(본인)기타 (암포함)질환<br/>약물치료여부 (0, 1)\n',
  `Q_FHX_DM` text COLLATE utf8_unicode_ci COMMENT '? 2002~2008년 문진<br/>(가족력)당뇨병유무 (1,2)',
  `G1E_BMI` double DEFAULT NULL,
  `G1E_WSTC` int(11) DEFAULT NULL,
  `G1E_WGHT` int(11) DEFAULT NULL COMMENT '체중 kg(20미만 결측처리)',
  `G1E_HGHT` int(11) DEFAULT NULL COMMENT '신장 cm(100미만 결측처리)',
  `G1E_VA_LT` double DEFAULT NULL COMMENT '시력(좌) 0.1~2.0 9.9: 실명\n',
  `G1E_VA_RT` double DEFAULT NULL COMMENT '시력(우) 0.1~2.0 9.9: 실명\n',
  `G1E_CRTN` double DEFAULT NULL COMMENT '혈청크레아티닌 mg/dL * 0값 결측처리\n',
  `G1E_HDL` int(11) DEFAULT NULL COMMENT 'HDL콜레스테롤 mg/dL * 0값 결측처리',
  `G1E_FBS` int(11) DEFAULT NULL COMMENT '식전혈당(공복혈당)<br/>mg/dL * 0값 결측처리\n',
  `G1E_TOT_CHOL` int(11) DEFAULT NULL COMMENT '총콜레스테롤 mg/dL * 0값 결측처리\n',
  `G1E_SGOT` int(11) DEFAULT NULL COMMENT '(혈청지오티)AST U/L * 0값 결측처리\n',
  `G1E_SGPT` int(11) DEFAULT NULL COMMENT '(혈청지피티)ALT<br/>U/L * 0값 결측처리\n',
  `G1E_HGB` double DEFAULT NULL COMMENT '혈색소 g/dL * 0값 결측처리\n',
  `G1E_TG` int(11) DEFAULT NULL COMMENT '트리글리세라이드 mg/dL * 0값 결측처리\n',
  `G1E_GGT` int(11) DEFAULT NULL COMMENT '감마지티피 U/L * 0값 결측처리\n',
  `G1E_LDL` int(11) DEFAULT NULL COMMENT 'LDL콜레스테롤 mg/dL * 0값 결측처리\n',
  `G1E_HA_LT` int(11) DEFAULT NULL,
  `G1E_URN_PROT` int(11) DEFAULT NULL,
  `G1E_BP_SYS` int(11) DEFAULT NULL,
  `G1E_CHST_XRAY_RST` int(11) DEFAULT NULL,
  `G1E_BP_DIA` int(11) DEFAULT NULL,
  `G1E_HA_RT` int(11) DEFAULT NULL,
  `G1E_GFR_MTHD` text COLLATE utf8_unicode_ci,
  `G1E_GFR` int(11) DEFAULT NULL,
  `Q_PA_VD` int(11) DEFAULT NULL COMMENT '? 2009~2015년 문진<br/>1주_20분이상 격렬한 운동<br/>0 : 0일<br/>1 : 1일<br/>2 : 2일<br/>3 : 3일<br/>4 : 4일<br/>5 : 5일<br/>6 : 6일<br/>7 : 7일\n',
  `Q_PA_MD` int(11) DEFAULT NULL COMMENT '? 2009~2015년 문진<br/>1주_30분이상중간정도 운동<br/>0 : 0일<br/>1 : 1일<br/>2 : 2일<br/>3 : 3일<br/>4 : 4일<br/>5 : 5일<br/>6 : 6일<br/>7 : 7일',
  `Q_PA_WALK` int(11) DEFAULT NULL COMMENT '? 2009~2015년 문진<br/>1주_총30분이상 걷기 운동<br/>0 : 0일<br/>1 : 1일<br/>2 : 2일<br/>3 : 3일<br/>4 : 4일<br/>5 : 5일<br/>6 : 6일<br/>7 : 7일',
  `Q_PHX_TX_STK` text COLLATE utf8_unicode_ci COMMENT '? 2009~2015년 문진<br/>(본인)뇌졸중<br/>약물치료여부 (0,1)',
  `Q_PHX_DX_ETC` text COLLATE utf8_unicode_ci COMMENT '? 2009~2015년 문진<br/>(본인)기타 (암포함)질환<br/>과거병력유무 (0,1)',
  `Q_PHX_TX_HTDZ` text COLLATE utf8_unicode_ci COMMENT '? 2009~2015년 문진<br/>(본인)심장병<br/>약물치료여부 (0,1)',
  `Q_FHX_ETC` text COLLATE utf8_unicode_ci COMMENT '? 2009~2015년 문진<br/>(가족력) 기타(암포함)환자유무 (0,1)',
  `Q_FHX_STK` text COLLATE utf8_unicode_ci COMMENT '? 2002~2008년 문진<br/>(가족력) 뇌졸중유무 (1,2)',
  `Q_SMK_NOW_DRT` text COLLATE utf8_unicode_ci COMMENT '? 2009~2015년 문진<br/>(현재) 흡연기간<br/>_년\n',
  `Q_SMK_PRE_AMT` text COLLATE utf8_unicode_ci COMMENT '? 2009~2015년 문진<br/>? 2009~2015년 문진<br/>_개피',
  `Q_PHX_DX_PTB` text COLLATE utf8_unicode_ci COMMENT '? 2009~2015년 문진<br/>(본인)폐결핵(2010~)<br/>과거병력유무 (0,1)',
  `Q_PHX_DX_DLD` text COLLATE utf8_unicode_ci COMMENT '? 2009~2015년 문진<br/>(본인)고지혈증 ( 이상지질혈증) (0,1)\n',
  `Q_HBV_AG` int(11) DEFAULT NULL COMMENT '? 2009~2015년 문진<br/>(본인)B형간염 항원보유자<br/>1: 예 2: 아니오 3: 모름',
  `Q_PHX_TX_PTB` text COLLATE utf8_unicode_ci COMMENT '(본인)폐결핵(2010~)<br/>약물치료여부 (0,1)',
  `Q_PHX_DX_DM` text COLLATE utf8_unicode_ci COMMENT '? 2009~2015년 문진<br/>(본인)당뇨병 과거병력유무 (0,1)',
  `Q_SMK_NOW_AMT_V09N` text COLLATE utf8_unicode_ci,
  `Q_PHX_TX_DLD` text COLLATE utf8_unicode_ci COMMENT '? 2009~2015년 문진<br/>(본인)고지혈증 ( 이상지질혈증)<br/>약물치료여부 (0,1)\n',
  `Q_FHX_HTDZ` text COLLATE utf8_unicode_ci COMMENT '? 2002~2008년 문진<br/>(가족력) 심장병유무 (1,2)',
  `Q_DRK_AMT_V09N` text COLLATE utf8_unicode_ci,
  `Q_DRK_FRQ_V09N` int(11) DEFAULT NULL,
  `Q_PHX_DX_STK` text COLLATE utf8_unicode_ci COMMENT '? 2009~2015년 문진<br/>본인)뇌졸중<br/>과거병력유무 (0,1)\n',
  `Q_SMK_PRE_DRT` text COLLATE utf8_unicode_ci COMMENT '? 2009~2015년 문진<br/>(과거) 흡연기간 __년',
  `Q_PHX_TX_HTN` text COLLATE utf8_unicode_ci COMMENT '? 2009~2015년 문진<br/>(본인)심근경색<br/>약물치료여부 (0,1)',
  `Q_PHX_DX_HTDZ` text COLLATE utf8_unicode_ci COMMENT '? 2009~2015년 문진<br/>(본인)심장병<br/>과거병력유무 (0,1)'
) ENGINE=InnoDB DEFAULT CHARSET=utf8 COLLATE=utf8_unicode_ci;

CREATE TABLE `nsc2_inst_1000` (
  `STD_YYYY` int(11) DEFAULT NULL COMMENT '기준년도 (해당연도)',
  `RN_INST` int(11) DEFAULT NULL COMMENT '요양기관 고유번호(6자리), 연결변수',
  `INST_CLSFC_CD` int(11) DEFAULT NULL COMMENT '요양기관 종별 코드',
  `SGG_INST` int(11) DEFAULT NULL COMMENT '요양기관 주소(시군구)정보',
  `DISP_SUBJ_TYPE` int(11) DEFAULT NULL COMMENT '표시 과목구분',
  `CNT_DR_TOT` int(11) DEFAULT NULL COMMENT '의사 수',
  `CNT_NRS_TOT` int(11) DEFAULT NULL COMMENT '간호사 수',
  `CNT_BED_INP` int(11) DEFAULT NULL COMMENT '입원병상 수',
  `CNT_BED_OP` int(11) DEFAULT NULL COMMENT '수술 병상 수',
  `CNT_BED_ER` int(11) DEFAULT NULL COMMENT '응급 병상 수'
) ENGINE=InnoDB DEFAULT CHARSET=utf8 COLLATE=utf8_unicode_ci COMMENT='요양 기관 테이블\n\n상급종합병원\n\n? 암, 중증질환 진료 등 전문적인 고난도 의료서비스를 제공하는 종\n합병원(2012년부터 3년 주기로 지정)\n?  보건복지부령으로 정하는 20개 이상의 진료과목을 갖추고 각 진료과목마다 전속하는 전문의를 둘 것\n?  전문의가 되려는 자를 수련시키는 기관일 것\n?  보건복지부령으로 정하는 인력·시설·장비 등을 갖출 것\n?  질병군별 환자구성 비율이 보건복지부령으로 정하는 기준에 해당할 것\n\n\n자료연결방법\n\n? 요양기관테이블을 제외한 전체 테이블 : 개인일련번호(RN_INDI)\n? 요양기관테이블 ↔진료테이블(20t) : 요양기관번호(RN_INST)\n? 진료테이블의 세부DB(20t, 30t, 40t, 60t) : 청구일련번호(RN_KEY)\n';

CREATE TABLE `nsc2_k20_1000` (
  `RN_INDI` int(11) DEFAULT NULL COMMENT '개인고유번호(7자리), 연계코드',
  `RN_KEY` bigint(20) DEFAULT NULL COMMENT '청구고유번호 (14자리), 연계코드',
  `RN_INST` int(11) DEFAULT NULL COMMENT '요양기관고유번호(6자리), 연계코드',
  `MDCARE_STRT_DT` int(11) DEFAULT NULL COMMENT '요양 게시일자\n\n- 수진자가 진료를 받기 시작한 일자\n- 의과, 치과, 한방, 보건기관: 당월 요양 개시 일자 (월 통합 작성 명세서) 또는 내원 일자\n- 약국: 조제 투약 일자',
  `FORM_CD` int(11) DEFAULT NULL COMMENT '서식코드\n\n요양급여비용심사(의료보호) 청수거 및 명세서의 진료 구분명세서 서식 구분',
  `MCARE_SUBJ_CD` int(11) DEFAULT NULL COMMENT '진료과목코드\n\n- (병원급 이상) 실제 진료를 받은 진료과목\n- (의원) 상병명에 해당하는 진료과목\n\n| 코드 | 진료과 |\n| --- | --- |\n| 00 | 일반의 |\n| 01 | 내과 |\n| 02 | 신경과 |\n| 03 | 정신과 | \n| 04 | 외과 | \n| 05 | 정형외과 | \n| 06 | 신경외과 | \n| 07 | 흉부외과 | \n| 08 | 성형외과 |\n| 09 | 마취통증의학과 | \n| 10 | 산부인과 |\n| 11 | 소아청소년과 |\n| 12 | 안과 | \n| 13 | 이비인후과 |\n| 14 | 피부과 |\n| 15 | 비뇨기과 |\n| 16 | 영상의학과 |\n| 17 | 방사선 종양학과 | \n| 18 | 병리과 |\n| 19 | 진단검사의학과 | \n| 20 | 결핵과 | \n| 21 | 재활의학과 | \n| 22 | 핵의학과 | \n| 23 | 가정의학과 | \n\n',
  `SICK_SYM1` varchar(10) COLLATE utf8_unicode_ci DEFAULT NULL COMMENT '주상병\n\n진료기간 중 치료나 검사 등에 대한 환자의 요구가 가장 컸던 상병',
  `SICK_SYM2` varchar(10) COLLATE utf8_unicode_ci DEFAULT NULL COMMENT '부상병\n\n진료기간 중 주상병과 함께 있었거나 발생된 상병으로 환자 진료에 영향을 주었던 상병',
  `FST_HSPTZ_DT` int(11) DEFAULT NULL COMMENT '최초 입원일자\n\n입원 요양급여비용 분리청구의 경우 입원의 최초 입원개시일 기재',
  `HSPTZ_PATH_TYPE` int(11) DEFAULT NULL COMMENT '입원 경로구분\n\n· 병원급 이상 입원 환자의 경우 기재\n· 1번째 자리(도착경로) 1: 타요양기관경유, 2: 응급구조대후송, 3: 기타\n· 2번째 자리(입원경로) 1 응급실, 2 외래\n',
  `OFIJ_TYPE` varchar(10) COLLATE utf8_unicode_ci DEFAULT NULL COMMENT '공상 등 구분\n\n· 공상(공무상 상해) 및 보훈, 군인, 차상위 희귀질환자 등에 해당되는\n적용대상자 구분 코드\n※ 각 제도별 대상자 선정기준 및 적용 내용은 관련 법령 및 고시 참고',
  `OPRTN_YN` int(11) DEFAULT NULL COMMENT '수술여부 (0: 미수술, 1: 수술)',
  `MDCARE_DD_CNT` int(11) DEFAULT NULL,
  `VSHSP_DD_CNT` int(11) DEFAULT NULL,
  `TOT_PRSC_DD_CNT` int(11) DEFAULT NULL COMMENT '총 처방일수\n\n· 처방전에 기재된 처방약품별 처방일수 중 가장 긴 처방일수\n· 처방일자별로 처방내역이 다른 경우에는 처방일수를 합산하여 기재\n',
  `MCARE_RSLT_TYPE` int(11) DEFAULT NULL COMMENT '진료결과구분\n\n· 최종진료일의 환자상태 구분\n· 1: 계속, 2: 이송, 3: 회송, 4 :사망, 9: 퇴원 또는 외래치료 종결\n',
  `EDC_ADD_RT` int(11) DEFAULT NULL COMMENT '심결 가산율\n\n· 요양기관의 종별 규모에 따라 시설, 인력, 장비 등의 투자비용\n등을 고려하여 가산 적용되는 비율(%)\n※ 요양개시일자 기준의 가산율, 공상가산율, 의료급여가산율 적용\n',
  `ED_RC_TOT_AMT` int(11) DEFAULT NULL COMMENT '심결 가산율\n\n· 요양기관의 종별 규모에 따라 시설, 인력, 장비 등의 투자비용\n등을 고려하여 가산 적용되는 비율(%)\n※ 요양개시일자 기준의 가산율, 공상가산율, 의료급여가산율 적용\n',
  `EDC_SBA` int(11) DEFAULT NULL COMMENT '심결 본인 부담금\n\n· 심사 결정 후 수진자가 부담해야 할 금액',
  `EDC_INSUR_BRDN_AMT` int(11) DEFAULT NULL COMMENT '심결 보험자 부담금\n\n· 심사결정 후 보험자가 부담해야 할 금액',
  `SPCF_SYM_TYPE` int(11) DEFAULT NULL COMMENT '특정기호구분\n\n「본인일부부담금 산정특례에 관한 기준」 관련 특정기호 코드',
  `STD_YYYY` int(11) DEFAULT NULL
) ENGINE=InnoDB DEFAULT CHARSET=utf8 COLLATE=utf8_unicode_ci COMMENT='한방 - 진료 명세서\n\n';

CREATE TABLE `nsc2_k30_1000` (
  `RN_INDI` int(11) DEFAULT NULL COMMENT '개인고유번호(7자리), 연계코드',
  `RN_KEY` bigint(20) DEFAULT NULL COMMENT '청구고유번호 (14자리), 연계코드',
  `MDCARE_STRT_DT` int(11) DEFAULT NULL COMMENT '요양 게시일자\n\n- 수진자가 진료를 받기 시작한 일자\n- 의과, 치과, 한방, 보건기관: 당월 요양 개시 일자 (월 통합 작성 명세서) 또는 내원 일자\n- 약국: 조제 투약 일자',
  `FORM_CD` int(11) DEFAULT NULL COMMENT '서식코드\n\n요양급여비용심사(의료보호) 청수거 및 명세서의 진료 구분명세서 서식 구분',
  `MCARE_DESC_LN_NO` int(11) DEFAULT NULL COMMENT '줄번호\n\n처방전 발행건에 대한 세부내역 일련번호',
  `CLA_CD` int(11) DEFAULT NULL COMMENT '항코드 (요양 급여 비용 명세서 항(1~10) 또는 C,M,T 코드), 코드표 참고\n',
  `ITEM_CD` int(11) DEFAULT NULL COMMENT '목코드 (18개항의 소분류 단위로 부여된 번호),  코드표 참고',
  `CLSF_PTTN_CD` varchar(10) COLLATE utf8_unicode_ci DEFAULT NULL COMMENT '분류유형코드\n\n명세서의 분류코드유형, 코드표 참고',
  `MCARE_DIV_CD` int(11) DEFAULT NULL COMMENT '진료분류코드\n\n진료분류코드(MCARE_DIV_CD) : 요양급여비용 분류항목별 코드\n?   수가(행위)코드, 약품코드, 치료재료코드 등 진료내역 관련 고유코드\n\n§ 해당 분류코드 확인은 수가파일, 약가파일 등 해당 파일을 다운받아 사용\nㄴ 건강보험심사평가원 홈페이지(hira.or.kr)\n\n※  약제의 경우 처방전교부상세(60T)의 일반명 코드(GNL_NM_CD)와 동일한 값으로 구성\n-  수가(행위)코드, 의약품 제품코드, 재료대코드 등이 포함되는데, 이 중 의약품 제품코드는 제품마다의 고유번호이기 때문에, 특정 약품에 대한 시장조사 등 사익 목적으로 이용될 가능성이 있어 기존 진료분류코드를 일반명코드 값으로 대체하였음\n',
  `UPRC` int(11) DEFAULT NULL COMMENT '단가 상세설명참고',
  `DD1_MQTY_FREQ` int(11) DEFAULT NULL COMMENT '1일투여량또는실시횟수\n\n1일 투여량을 기재, 의약품 및 처방내역 의약품의 경우 1일 투약횟수 기재\n',
  `TOT_MCNT` int(11) DEFAULT NULL COMMENT '총투여일수 또는 실시횟수\n',
  `AMT` int(11) DEFAULT NULL COMMENT '금액\n\n단가 × (일일투여량 또는 실시횟수) × (총투여일수 또는 실시횟수)',
  `TIME1_MDCT_CPCT` int(11) DEFAULT NULL COMMENT '1회 투여 용량',
  `MDCN_UD` int(11) DEFAULT NULL COMMENT '약제상한차액\n\n약제의 상한가와 요양기관이 구입한 단가와의 차액 중\n100분의70에 해당하는 금액....※2010.11월 차수 추가\n',
  `MCEXP_TYPE_CD` int(11) DEFAULT NULL COMMENT '진료비 구분 코드\n\n1: 기본진료료\n2: 진료행위료\n3: 약품비\n4: 치료재료료\n5: 정액수가\n',
  `EFMDC_CLSF_NO` int(11) DEFAULT NULL COMMENT '약효 분류번호\n\n· (표본코호트 2.0 DB 추가) ‘주성분코드’ 활용 권장'
) ENGINE=InnoDB DEFAULT CHARSET=utf8 COLLATE=utf8_unicode_ci COMMENT='진료내역';

CREATE TABLE `nsc2_k40_1000` (
  `RN_INDI` int(11) DEFAULT NULL COMMENT '개인고유번호(7자리), 연계코드',
  `RN_KEY` bigint(20) DEFAULT NULL COMMENT '청구고유번호 (14자리), 연계코드',
  `MDCARE_STRT_DT` int(11) DEFAULT NULL COMMENT '요양 게시일자\n\n- 수진자가 진료를 받기 시작한 일자\n- 의과, 치과, 한방, 보건기관: 당월 요양 개시 일자 (월 통합 작성 명세서) 또는 내원 일자\n- 약국: 조제 투약 일자',
  `FORM_CD` int(11) DEFAULT NULL COMMENT '서식코드\n\n요양급여비용심사(의료보호) 청수거 및 명세서의 진료 구분명세서 서식 구분',
  `MCEX_SICK_SYM` varchar(10) COLLATE utf8_unicode_ci DEFAULT NULL COMMENT '요양급여상병기호\n\n진료기간 중 환자가 진료 받은 해당상병',
  `DETAIL_TMSG_SUBJ_CD` int(11) DEFAULT NULL COMMENT '세부전문과목코드\n',
  `SICK_CLSF_TYPE` int(11) DEFAULT NULL COMMENT '상병분류구분코드\n\n· 각 상병기호별 주상병, 부상병, 배제된 상병을 구분하는 구분자\n1: 주상병\n2: 부상병\n3: 배제된 상병 (결측값도 존재)\n(조건 설정:  1, 2, 결측 값 모두 선정)\n',
  `STD_YYYY` int(11) DEFAULT NULL
) ENGINE=InnoDB DEFAULT CHARSET=utf8 COLLATE=utf8_unicode_ci COMMENT='상병내역';

CREATE TABLE `nsc2_m20_1000` (
  `RN_INDI` int(11) DEFAULT NULL COMMENT '개인고유번호(7자리), 연계코드',
  `RN_KEY` bigint(20) DEFAULT NULL COMMENT '청구고유번호 (14자리), 연계코드',
  `RN_INST` int(11) DEFAULT NULL COMMENT '요양기관고유번호(6자리), 연계코드',
  `MDCARE_STRT_DT` int(11) DEFAULT NULL COMMENT '요양 게시일자\n\n- 수진자가 진료를 받기 시작한 일자\n- 의과, 치과, 한방, 보건기관: 당월 요양 개시 일자 (월 통합 작성 명세서) 또는 내원 일자\n- 약국: 조제 투약 일자',
  `FORM_CD` int(11) DEFAULT NULL COMMENT '서식코드\n\n요양급여비용심사(의료보호) 청수거 및 명세서의 진료 구분명세서 서식 구분',
  `MCARE_SUBJ_CD` int(11) DEFAULT NULL COMMENT '진료과목코드\n\n- (병원급 이상) 실제 진료를 받은 진료과목\n- (의원) 상병명에 해당하는 진료과목\n\n| 코드 | 진료과 |\n| --- | --- |\n| 00 | 일반의 |\n| 01 | 내과 |\n| 02 | 신경과 |\n| 03 | 정신과 | \n| 04 | 외과 | \n| 05 | 정형외과 | \n| 06 | 신경외과 | \n| 07 | 흉부외과 | \n| 08 | 성형외과 |\n| 09 | 마취통증의학과 | \n| 10 | 산부인과 |\n| 11 | 소아청소년과 |\n| 12 | 안과 | \n| 13 | 이비인후과 |\n| 14 | 피부과 |\n| 15 | 비뇨기과 |\n| 16 | 영상의학과 |\n| 17 | 방사선 종양학과 | \n| 18 | 병리과 |\n| 19 | 진단검사의학과 | \n| 20 | 결핵과 | \n| 21 | 재활의학과 | \n| 22 | 핵의학과 | \n| 23 | 가정의학과 | \n\n',
  `SICK_SYM1` varchar(10) COLLATE utf8_unicode_ci DEFAULT NULL COMMENT '주상병\n\n진료기간 중 치료나 검사 등에 대한 환자의 요구가 가장 컸던 상병',
  `SICK_SYM2` varchar(10) COLLATE utf8_unicode_ci DEFAULT NULL COMMENT '부상병\n\n진료기간 중 주상병과 함께 있었거나 발생된 상병으로 환자 진료에 영향을 주었던 상병',
  `FST_HSPTZ_DT` int(11) DEFAULT NULL COMMENT '최초 입원일자\n\n입원 요양급여비용 분리청구의 경우 입원의 최초 입원개시일 기재',
  `HSPTZ_PATH_TYPE` int(11) DEFAULT NULL COMMENT '입원 경로구분\n\n· 병원급 이상 입원 환자의 경우 기재\n· 1번째 자리(도착경로) 1: 타요양기관경유, 2: 응급구조대후송, 3: 기타\n· 2번째 자리(입원경로) 1 응급실, 2 외래\n',
  `OFIJ_TYPE` int(10) DEFAULT NULL COMMENT '공상 등 구분\n\n· 공상(공무상 상해) 및 보훈, 군인, 차상위 희귀질환자 등에 해당되는\n적용대상자 구분 코드\n※ 각 제도별 대상자 선정기준 및 적용 내용은 관련 법령 및 고시 참고',
  `OPRTN_YN` int(11) DEFAULT NULL COMMENT '수술여부 (0: 미수술, 1: 수술)',
  `MDCARE_DD_CNT` int(11) DEFAULT NULL,
  `VSHSP_DD_CNT` int(11) DEFAULT NULL,
  `TOT_PRSC_DD_CNT` int(11) DEFAULT NULL COMMENT '총 처방일수\n\n· 처방전에 기재된 처방약품별 처방일수 중 가장 긴 처방일수\n· 처방일자별로 처방내역이 다른 경우에는 처방일수를 합산하여 기재\n',
  `MCARE_RSLT_TYPE` int(11) DEFAULT NULL COMMENT '진료결과구분\n\n· 최종진료일의 환자상태 구분\n· 1: 계속, 2: 이송, 3: 회송, 4 :사망, 9: 퇴원 또는 외래치료 종결\n',
  `EDC_ADD_RT` float DEFAULT NULL COMMENT '심결 가산율\n\n· 요양기관의 종별 규모에 따라 시설, 인력, 장비 등의 투자비용\n등을 고려하여 가산 적용되는 비율(%)\n※ 요양개시일자 기준의 가산율, 공상가산율, 의료급여가산율 적용\n',
  `ED_RC_TOT_AMT` int(11) DEFAULT NULL COMMENT '심결요양 급여 비용 총액\n\n· 심사결정 후 건강보험 총 요양 급여 비용',
  `EDC_SBA` int(11) DEFAULT NULL COMMENT '심결 본인 부담금\n\n· 심사 결정 후 수진자가 부담해야 할 금액',
  `EDC_INSUR_BRDN_AMT` int(11) DEFAULT NULL COMMENT '심결 보험자 부담금\n\n· 심사결정 후 보험자가 부담해야 할 금액',
  `SPCF_SYM_TYPE` varchar(10) COLLATE utf8_unicode_ci DEFAULT NULL COMMENT '특정기호구분\n\n「본인일부부담금 산정특례에 관한 기준」 관련 특정기호 코드',
  `STD_YYYY` int(11) DEFAULT NULL
) ENGINE=InnoDB DEFAULT CHARSET=utf8 COLLATE=utf8_unicode_ci COMMENT='외과 보건기관 - 진료 명세서\n\n';

CREATE TABLE `nsc2_m30_1000` (
  `RN_INDI` int(11) DEFAULT NULL COMMENT '개인고유번호(7자리), 연계코드',
  `RN_KEY` bigint(20) DEFAULT NULL COMMENT '청구고유번호 (14자리), 연계코드',
  `MDCARE_STRT_DT` int(11) DEFAULT NULL COMMENT '요양 게시일자\n\n- 수진자가 진료를 받기 시작한 일자\n- 의과, 치과, 한방, 보건기관: 당월 요양 개시 일자 (월 통합 작성 명세서) 또는 내원 일자\n- 약국: 조제 투약 일자',
  `FORM_CD` int(11) DEFAULT NULL COMMENT '서식코드\n\n요양급여비용심사(의료보호) 청수거 및 명세서의 진료 구분명세서 서식 구분',
  `MCARE_DESC_LN_NO` int(11) DEFAULT NULL COMMENT '줄번호\n\n처방전 발행건에 대한 세부내역 일련번호',
  `CLA_CD` int(11) DEFAULT NULL COMMENT '항코드 (요양 급여 비용 명세서 항(1~10) 또는 C,M,T 코드), 코드표 참고\n',
  `ITEM_CD` int(11) DEFAULT NULL COMMENT '목코드 (18개항의 소분류 단위로 부여된 번호),  코드표 참고',
  `CLSF_PTTN_CD` int(11) DEFAULT NULL COMMENT '분류유형코드\n\n명세서의 분류코드유형, 코드표 참고',
  `MCARE_DIV_CD` varchar(10) COLLATE utf8_unicode_ci DEFAULT NULL COMMENT '진료분류코드\n\n진료분류코드(MCARE_DIV_CD) : 요양급여비용 분류항목별 코드\n?   수가(행위)코드, 약품코드, 치료재료코드 등 진료내역 관련 고유코드\n\n§ 해당 분류코드 확인은 수가파일, 약가파일 등 해당 파일을 다운받아 사용\nㄴ 건강보험심사평가원 홈페이지(hira.or.kr)\n\n※  약제의 경우 처방전교부상세(60T)의 일반명 코드(GNL_NM_CD)와 동일한 값으로 구성\n-  수가(행위)코드, 의약품 제품코드, 재료대코드 등이 포함되는데, 이 중 의약품 제품코드는 제품마다의 고유번호이기 때문에, 특정 약품에 대한 시장조사 등 사익 목적으로 이용될 가능성이 있어 기존 진료분류코드를 일반명코드 값으로 대체하였음\n',
  `UPRC` int(11) DEFAULT NULL COMMENT '단가 상세설명참고',
  `DD1_MQTY_FREQ` int(11) DEFAULT NULL COMMENT '1일투여량또는실시횟수\n\n1일 투여량을 기재, 의약품 및 처방내역 의약품의 경우 1일 투약횟수 기재\n',
  `TOT_MCNT` int(11) DEFAULT NULL COMMENT '총투여일수 또는 실시횟수\n',
  `AMT` int(11) DEFAULT NULL COMMENT '금액\n\n단가 × (일일투여량 또는 실시횟수) × (총투여일수 또는 실시횟수)',
  `TIME1_MDCT_CPCT` int(11) DEFAULT NULL COMMENT '1회 투여 용량',
  `MDCN_UD` int(11) DEFAULT NULL COMMENT '약제상한차액\n\n약제의 상한가와 요양기관이 구입한 단가와의 차액 중\n100분의70에 해당하는 금액....※2010.11월 차수 추가\n',
  `MCEXP_TYPE_CD` int(11) DEFAULT NULL COMMENT '진료비 구분 코드\n\n1: 기본진료료\n2: 진료행위료\n3: 약품비\n4: 치료재료료\n5: 정액수가\n',
  `EFMDC_CLSF_NO` int(11) DEFAULT NULL COMMENT '약효 분류번호\n\n· (표본코호트 2.0 DB 추가) ‘주성분코드’ 활용 권장'
) ENGINE=InnoDB DEFAULT CHARSET=utf8 COLLATE=utf8_unicode_ci COMMENT='진료내역';

CREATE TABLE `nsc2_m40_1000` (
  `RN_INDI` int(11) DEFAULT NULL COMMENT '개인고유번호(7자리), 연계코드',
  `RN_KEY` bigint(20) DEFAULT NULL COMMENT '청구고유번호 (14자리), 연계코드',
  `MDCARE_STRT_DT` int(11) DEFAULT NULL COMMENT '요양 게시일자\n\n- 수진자가 진료를 받기 시작한 일자\n- 의과, 치과, 한방, 보건기관: 당월 요양 개시 일자 (월 통합 작성 명세서) 또는 내원 일자\n- 약국: 조제 투약 일자',
  `FORM_CD` int(11) DEFAULT NULL COMMENT '서식코드\n\n요양급여비용심사(의료보호) 청수거 및 명세서의 진료 구분명세서 서식 구분',
  `MCEX_SICK_SYM` varchar(10) COLLATE utf8_unicode_ci DEFAULT NULL COMMENT '요양급여상병기호\n\n진료기간 중 환자가 진료 받은 해당상병',
  `DETAIL_TMSG_SUBJ_CD` varchar(10) COLLATE utf8_unicode_ci DEFAULT NULL COMMENT '세부전문과목코드\n',
  `SICK_CLSF_TYPE` varchar(10) COLLATE utf8_unicode_ci DEFAULT NULL COMMENT '상병분류구분코드\n\n· 각 상병기호별 주상병, 부상병, 배제된 상병을 구분하는 구분자\n1: 주상병\n2: 부상병\n3: 배제된 상병 (결측값도 존재)\n(조건 설정:  1, 2, 결측 값 모두 선정)\n',
  `STD_YYYY` int(11) DEFAULT NULL
) ENGINE=InnoDB DEFAULT CHARSET=utf8 COLLATE=utf8_unicode_ci COMMENT='상병내역';

CREATE TABLE `nsc2_m60_1000` (
  `RN_INDI` int(11) DEFAULT NULL COMMENT '개인고유번호(7자리), 연계코드',
  `RN_KEY` bigint(20) DEFAULT NULL COMMENT '청구고유번호 (14자리), 연계코드',
  `MDCARE_STRT_DT` int(11) DEFAULT NULL COMMENT '요양 게시일자\n\n- 수진자가 진료를 받기 시작한 일자\n- 의과, 치과, 한방, 보건기관: 당월 요양 개시 일자 (월 통합 작성 명세서) 또는 내원 일자\n- 약국: 조제 투약 일자',
  `FORM_CD` int(11) DEFAULT NULL COMMENT '서식코드\n\n요양급여비용심사(의료보호) 청수거 및 명세서의 진료 구분명세서 서식 구분',
  `MCARE_DESC_LN_NO` int(11) DEFAULT NULL COMMENT '줄번호\n\n처방전발행건 세부내역 일련번호\n',
  `CLSF_PTTN_CD` int(11) DEFAULT NULL COMMENT '분류 유형코드\n\n명세서의 분류코드유형, 코드표 참고',
  `MPRSC_TIME1_TUYAK_CPCT` int(11) DEFAULT NULL COMMENT '1회투여량횟수\n\n1회 투약량을 기재',
  `MPRSC_DD1_TUYAK_CPCT` int(11) DEFAULT NULL COMMENT '1일 투약량\n\n1회 투약량을 기재',
  `TOT_MCNT` int(11) DEFAULT NULL COMMENT '총 투여 일수',
  `UPRC` int(11) DEFAULT NULL COMMENT '단가',
  `AMT` int(11) DEFAULT NULL COMMENT '금액 (1회 투여량 × 1일 투약량 × 총 투여일수 × 단가)',
  `GNL_NM_CD` varchar(10) COLLATE utf8_unicode_ci DEFAULT NULL,
  `EFMDC_CLSF_NO` int(11) DEFAULT NULL COMMENT '약효 분류번호 (진료내역(T30) 설명 참고)'
) ENGINE=InnoDB DEFAULT CHARSET=utf8 COLLATE=utf8_unicode_ci COMMENT='처방전 교부 상세';

CREATE TABLE `nsc2_p20_1000` (
  `RN_INDI` int(11) DEFAULT NULL COMMENT '개인고유번호(7자리), 연계코드',
  `RN_KEY` bigint(20) DEFAULT NULL COMMENT '청구고유번호 (14자리), 연계코드',
  `RN_INST` int(11) DEFAULT NULL COMMENT '요양기관고유번호(6자리), 연계코드',
  `MDCARE_STRT_DT` int(11) DEFAULT NULL COMMENT '요양 게시일자\n\n- 수진자가 진료를 받기 시작한 일자\n- 의과, 치과, 한방, 보건기관: 당월 요양 개시 일자 (월 통합 작성 명세서) 또는 내원 일자\n- 약국: 조제 투약 일자',
  `FORM_CD` int(11) DEFAULT NULL COMMENT '서식코드\n\n요양급여비용심사(의료보호) 청수거 및 명세서의 진료 구분명세서 서식 구분',
  `MCARE_SUBJ_CD` int(11) DEFAULT NULL COMMENT '진료과목코드\n\n- (병원급 이상) 실제 진료를 받은 진료과목\n- (의원) 상병명에 해당하는 진료과목\n\n| 코드 | 진료과 |\n| --- | --- |\n| 00 | 일반의 |\n| 01 | 내과 |\n| 02 | 신경과 |\n| 03 | 정신과 | \n| 04 | 외과 | \n| 05 | 정형외과 | \n| 06 | 신경외과 | \n| 07 | 흉부외과 | \n| 08 | 성형외과 |\n| 09 | 마취통증의학과 | \n| 10 | 산부인과 |\n| 11 | 소아청소년과 |\n| 12 | 안과 | \n| 13 | 이비인후과 |\n| 14 | 피부과 |\n| 15 | 비뇨기과 |\n| 16 | 영상의학과 |\n| 17 | 방사선 종양학과 | \n| 18 | 병리과 |\n| 19 | 진단검사의학과 | \n| 20 | 결핵과 | \n| 21 | 재활의학과 | \n| 22 | 핵의학과 | \n| 23 | 가정의학과 | \n\n',
  `SICK_SYM1` varchar(10) COLLATE utf8_unicode_ci DEFAULT NULL COMMENT '주상병\n\n진료기간 중 치료나 검사 등에 대한 환자의 요구가 가장 컸던 상병',
  `SICK_SYM2` varchar(10) COLLATE utf8_unicode_ci DEFAULT NULL COMMENT '부상병\n\n진료기간 중 주상병과 함께 있었거나 발생된 상병으로 환자 진료에 영향을 주었던 상병',
  `FST_HSPTZ_DT` int(11) DEFAULT NULL COMMENT '최초 입원일자\n\n입원 요양급여비용 분리청구의 경우 입원의 최초 입원개시일 기재',
  `HSPTZ_PATH_TYPE` int(11) DEFAULT NULL COMMENT '입원 경로구분\n\n· 병원급 이상 입원 환자의 경우 기재\n· 1번째 자리(도착경로) 1: 타요양기관경유, 2: 응급구조대후송, 3: 기타\n· 2번째 자리(입원경로) 1 응급실, 2 외래\n',
  `OFIJ_TYPE` varchar(10) COLLATE utf8_unicode_ci DEFAULT NULL COMMENT '공상 등 구분\n\n· 공상(공무상 상해) 및 보훈, 군인, 차상위 희귀질환자 등에 해당되는\n적용대상자 구분 코드\n※ 각 제도별 대상자 선정기준 및 적용 내용은 관련 법령 및 고시 참고',
  `OPRTN_YN` int(11) DEFAULT NULL COMMENT '수술여부 (0: 미수술, 1: 수술)',
  `MDCARE_DD_CNT` int(11) DEFAULT NULL,
  `VSHSP_DD_CNT` int(11) DEFAULT NULL,
  `TOT_PRSC_DD_CNT` int(11) DEFAULT NULL COMMENT '총 처방일수\n\n· 처방전에 기재된 처방약품별 처방일수 중 가장 긴 처방일수\n· 처방일자별로 처방내역이 다른 경우에는 처방일수를 합산하여 기재\n',
  `MCARE_RSLT_TYPE` int(11) DEFAULT NULL COMMENT '진료결과구분\n\n· 최종진료일의 환자상태 구분\n· 1: 계속, 2: 이송, 3: 회송, 4 :사망, 9: 퇴원 또는 외래치료 종결\n',
  `EDC_ADD_RT` int(11) DEFAULT NULL COMMENT '심결 가산율\n\n· 요양기관의 종별 규모에 따라 시설, 인력, 장비 등의 투자비용\n등을 고려하여 가산 적용되는 비율(%)\n※ 요양개시일자 기준의 가산율, 공상가산율, 의료급여가산율 적용\n',
  `ED_RC_TOT_AMT` int(11) DEFAULT NULL COMMENT '심결 가산율\n\n· 요양기관의 종별 규모에 따라 시설, 인력, 장비 등의 투자비용\n등을 고려하여 가산 적용되는 비율(%)\n※ 요양개시일자 기준의 가산율, 공상가산율, 의료급여가산율 적용\n',
  `EDC_SBA` int(11) DEFAULT NULL COMMENT '심결 본인 부담금\n\n· 심사 결정 후 수진자가 부담해야 할 금액',
  `EDC_INSUR_BRDN_AMT` int(11) DEFAULT NULL COMMENT '심결 보험자 부담금\n\n· 심사결정 후 보험자가 부담해야 할 금액',
  `SPCF_SYM_TYPE` varchar(10) COLLATE utf8_unicode_ci DEFAULT NULL COMMENT '특정기호구분\n\n「본인일부부담금 산정특례에 관한 기준」 관련 특정기호 코드',
  `STD_YYYY` int(11) DEFAULT NULL
) ENGINE=InnoDB DEFAULT CHARSET=utf8 COLLATE=utf8_unicode_ci COMMENT='약국 - 진료 명세서';

CREATE TABLE `nsc2_p30_0208_1000` (
  `RN_INDI` int(11) DEFAULT NULL,
  `RN_KEY` bigint(20) DEFAULT NULL COMMENT '청구고유번호 (14자리), 연계코드',
  `MDCARE_STRT_DT` int(11) DEFAULT NULL COMMENT '요양 게시일자\n\n- 수진자가 진료를 받기 시작한 일자\n- 의과, 치과, 한방, 보건기관: 당월 요양 개시 일자 (월 통합 작성 명세서) 또는 내원 일자\n- 약국: 조제 투약 일자',
  `FORM_CD` int(11) DEFAULT NULL COMMENT '서식코드\n\n요양급여비용심사(의료보호) 청수거 및 명세서의 진료 구분명세서 서식 구분',
  `MCARE_DESC_LN_NO` int(11) DEFAULT NULL COMMENT '줄번호\n\n처방전 발행건에 대한 세부내역 일련번호',
  `CLA_CD` int(11) DEFAULT NULL COMMENT '항코드 (요양 급여 비용 명세서 항(1~10) 또는 C,M,T 코드), 코드표 참고\n',
  `ITEM_CD` int(11) DEFAULT NULL COMMENT '목코드 (18개항의 소분류 단위로 부여된 번호),  코드표 참고',
  `CLSF_PTTN_CD` int(11) DEFAULT NULL COMMENT '분류유형코드\n\n명세서의 분류코드유형, 코드표 참고',
  `MCARE_DIV_CD` varchar(10) COLLATE utf8_unicode_ci DEFAULT NULL COMMENT '진료분류코드\n\n진료분류코드(MCARE_DIV_CD) : 요양급여비용 분류항목별 코드\n?   수가(행위)코드, 약품코드, 치료재료코드 등 진료내역 관련 고유코드\n\n§ 해당 분류코드 확인은 수가파일, 약가파일 등 해당 파일을 다운받아 사용\nㄴ 건강보험심사평가원 홈페이지(hira.or.kr)\n\n※  약제의 경우 처방전교부상세(60T)의 일반명 코드(GNL_NM_CD)와 동일한 값으로 구성\n-  수가(행위)코드, 의약품 제품코드, 재료대코드 등이 포함되는데, 이 중 의약품 제품코드는 제품마다의 고유번호이기 때문에, 특정 약품에 대한 시장조사 등 사익 목적으로 이용될 가능성이 있어 기존 진료분류코드를 일반명코드 값으로 대체하였음\n',
  `UPRC` int(11) DEFAULT NULL COMMENT '단가 상세설명참고',
  `DD1_MQTY_FREQ` int(11) DEFAULT NULL COMMENT '1일투여량또는실시횟수\n\n1일 투여량을 기재, 의약품 및 처방내역 의약품의 경우 1일 투약횟수 기재\n',
  `TOT_MCNT` int(11) DEFAULT NULL COMMENT '총투여일수 또는 실시횟수\n',
  `AMT` int(11) DEFAULT NULL COMMENT '금액\n\n단가 × (일일투여량 또는 실시횟수) × (총투여일수 또는 실시횟수)',
  `TIME1_MDCT_CPCT` int(11) DEFAULT NULL COMMENT '1회 투여 용량',
  `MDCN_UD` int(11) DEFAULT NULL COMMENT '약제상한차액\n\n약제의 상한가와 요양기관이 구입한 단가와의 차액 중\n100분의70에 해당하는 금액....※2010.11월 차수 추가\n',
  `MCEXP_TYPE_CD` int(11) DEFAULT NULL COMMENT '진료비 구분 코드\n\n1: 기본진료료\n2: 진료행위료\n3: 약품비\n4: 치료재료료\n5: 정액수가\n',
  `EFMDC_CLSF_NO` int(11) DEFAULT NULL COMMENT '약효 분류번호\n\n· (표본코호트 2.0 DB 추가) ‘주성분코드’ 활용 권장'
) ENGINE=InnoDB DEFAULT CHARSET=utf8 COLLATE=utf8_unicode_ci COMMENT='진료내역';

CREATE TABLE `nsc2_p30_0915_1000` (
  `RN_INDI` int(11) DEFAULT NULL,
  `RN_KEY` bigint(20) DEFAULT NULL COMMENT '청구고유번호 (14자리), 연계코드',
  `MDCARE_STRT_DT` int(11) DEFAULT NULL COMMENT '요양 게시일자\n\n- 수진자가 진료를 받기 시작한 일자\n- 의과, 치과, 한방, 보건기관: 당월 요양 개시 일자 (월 통합 작성 명세서) 또는 내원 일자\n- 약국: 조제 투약 일자',
  `FORM_CD` int(11) DEFAULT NULL COMMENT '서식코드\n\n요양급여비용심사(의료보호) 청수거 및 명세서의 진료 구분명세서 서식 구분',
  `MCARE_DESC_LN_NO` int(11) DEFAULT NULL COMMENT '줄번호\n\n처방전 발행건에 대한 세부내역 일련번호',
  `CLA_CD` int(11) DEFAULT NULL COMMENT '항코드 (요양 급여 비용 명세서 항(1~10) 또는 C,M,T 코드), 코드표 참고\n',
  `ITEM_CD` int(11) DEFAULT NULL COMMENT '목코드 (18개항의 소분류 단위로 부여된 번호),  코드표 참고',
  `CLSF_PTTN_CD` int(11) DEFAULT NULL COMMENT '분류유형코드\n\n명세서의 분류코드유형, 코드표 참고',
  `MCARE_DIV_CD` varchar(10) COLLATE utf8_unicode_ci DEFAULT NULL COMMENT '진료분류코드\n\n진료분류코드(MCARE_DIV_CD) : 요양급여비용 분류항목별 코드\n?   수가(행위)코드, 약품코드, 치료재료코드 등 진료내역 관련 고유코드\n\n§ 해당 분류코드 확인은 수가파일, 약가파일 등 해당 파일을 다운받아 사용\nㄴ 건강보험심사평가원 홈페이지(hira.or.kr)\n\n※  약제의 경우 처방전교부상세(60T)의 일반명 코드(GNL_NM_CD)와 동일한 값으로 구성\n-  수가(행위)코드, 의약품 제품코드, 재료대코드 등이 포함되는데, 이 중 의약품 제품코드는 제품마다의 고유번호이기 때문에, 특정 약품에 대한 시장조사 등 사익 목적으로 이용될 가능성이 있어 기존 진료분류코드를 일반명코드 값으로 대체하였음\n',
  `UPRC` int(11) DEFAULT NULL COMMENT '단가 상세설명참고',
  `DD1_MQTY_FREQ` int(11) DEFAULT NULL COMMENT '1일투여량또는실시횟수\n\n1일 투여량을 기재, 의약품 및 처방내역 의약품의 경우 1일 투약횟수 기재\n',
  `TOT_MCNT` int(11) DEFAULT NULL COMMENT '총투여일수 또는 실시횟수',
  `AMT` int(11) DEFAULT NULL COMMENT '금액\n\n단가 × (일일투여량 또는 실시횟수) × (총투여일수 또는 실시횟수)',
  `TIME1_MDCT_CPCT` int(11) DEFAULT NULL COMMENT '1회 투여 용량',
  `MDCN_UD` int(11) DEFAULT NULL COMMENT '약제상한차액\n\n약제의 상한가와 요양기관이 구입한 단가와의 차액 중\n100분의70에 해당하는 금액....※2010.11월 차수 추가',
  `MCEXP_TYPE_CD` int(11) DEFAULT NULL COMMENT '진료비 구분 코드\n\n1: 기본진료료\n2: 진료행위료\n3: 약품비\n4: 치료재료료\n5: 정액수가',
  `EFMDC_CLSF_NO` int(11) DEFAULT NULL COMMENT '약효 분류번호\n\n· (표본코호트 2.0 DB 추가) ‘주성분코드’ 활용 권장'
) ENGINE=InnoDB DEFAULT CHARSET=utf8 COLLATE=utf8_unicode_ci COMMENT='약국 - 진료내역';
```

